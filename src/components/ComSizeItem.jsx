const ComSizeItem = ({ title }) => {
  return (
    <p className="bg-white flex items-center justify-center gap-2 cursor-pointer px-5 py-1 border rounded border-gray-700">
      {title}
    </p>
  );
};

export default ComSizeItem;
